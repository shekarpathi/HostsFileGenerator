name: Shell

on: [push]

defaults:
  run:
    working-directory: /home/runner/work/helloWorld/initial

jobs:
  my-job:
    name: My Job
    runs-on: ubuntu-latest
    steps:
    - name: Print a greeting
      run: |
        echo Hello there!
